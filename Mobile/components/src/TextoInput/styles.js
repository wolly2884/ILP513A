import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  contorno:{
    marginTop: 20,
    marginStart: 15,
    marginEnd: 15,
    width: '60%',
    height: '20%',
    alignSelf: 'center'
  }
})

export {styles}